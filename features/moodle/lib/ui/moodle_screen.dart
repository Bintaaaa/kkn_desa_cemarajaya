import 'package:flutter/material.dart';

class MoodleScreen extends StatelessWidget {
  const MoodleScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: SafeArea(
        child: Text("ini moodle"),
      ),
    );
  }
}
