import 'package:flutter/material.dart';

class TraderScreen extends StatelessWidget {
  const TraderScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: SafeArea(
        child: Text("ini trader"),
      ),
    );
  }
}
